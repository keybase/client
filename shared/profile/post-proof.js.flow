// @flow
import {Component} from 'react'

import type {ProvablePlatformsType} from '../constants/types/more'
import type {IconType} from '../common-adapters/icon'

export type Props = {
  allowProofCheck: boolean,
  platform: ProvablePlatformsType,
  platformUserName: string,
  descriptionText?: string,
  proofText?: ?string,
  proofAction?: () => void,
  proofActionText?: ?string,
  onAllowProofCheck: boolean => void,
  onCancel: () => void,
  onCancelText?: ?string,
  onComplete: () => void,
  isOnCompleteWaiting?: boolean,
  errorMessage?: ?string,
}

export default class Render extends Component<Props> {}
