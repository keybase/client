/* @flow */

export type Folder = {
  users: Array<{
    username: string,
    broken?: boolean
  }>,
  meta?: 'new' | null,
  modified?: {
    when: string,
    username: string
  },
  isPublic: boolean,
  ignored: boolean,
  isFirst: boolean
}

export type Props = {
  tlfs?: Array<Folder>,
  ignored?: Array<Folder>,
  isPublic: boolean,
  privateBadge: number,
  publicBadge: number
}

export default class Render extends React$Component<void, Props, void> {
}
