// Intentionally-empty ESM module. Re-exported via `export *` from empty-esm.js so
// es-module-lexer sees an opaque export list and the Vite dev server skips its
// strict named-export check.
export {}
