diff --git a/node_modules/expo/src/async-require/loadBundle.ts b/node_modules/expo/src/async-require/loadBundle.ts
index eb76d36..5d9ea8d 100644
--- a/node_modules/expo/src/async-require/loadBundle.ts
+++ b/node_modules/expo/src/async-require/loadBundle.ts
@@ -19,8 +19,12 @@ export async function loadBundleAsync(bundlePath: string): Promise<void> {
     return fetchThenEvalAsync(requestUrl);
   } else {
     return fetchThenEvalAsync(requestUrl).then(() => {
-      const HMRClient = require('./hmr').default as typeof import('./hmr').default;
-      HMRClient.registerBundle(requestUrl);
+      // On native, the bridge manages HMR registration directly.
+      // Expo's HMRClient wrapper is only initialized on web.
+      if (process.env.EXPO_OS === 'web') {
+        const HMRClient = require('./hmr').default as typeof import('./hmr').default;
+        HMRClient.registerBundle(requestUrl);
+      }
     });
   }
 }
diff --git a/node_modules/expo/src/async-require/setup.ts b/node_modules/expo/src/async-require/setup.ts
index e530acf..7f5e03f 100644
--- a/node_modules/expo/src/async-require/setup.ts
+++ b/node_modules/expo/src/async-require/setup.ts
@@ -5,6 +5,10 @@ if (
   typeof window !== 'undefined'
 ) {
   require('./setupFastRefresh');
-  require('./setupHMR');
+  // setupHMR calls HMRClient.setup({ isEnabled: true }) which only works on web.
+  // On native, the bridge calls HMRClient.setup() with platform-specific args.
+  if (process.env.EXPO_OS === 'web') {
+    require('./setupHMR');
+  }
   require('./messageSocket');
 }
