diff --git a/node_modules/react-native-screens/lib/commonjs/components/tabs/screen/TabsScreen.ios.js b/node_modules/react-native-screens/lib/commonjs/components/tabs/screen/TabsScreen.ios.js
index 2bd1918..f456b9c 100644
--- a/node_modules/react-native-screens/lib/commonjs/components/tabs/screen/TabsScreen.ios.js
+++ b/node_modules/react-native-screens/lib/commonjs/components/tabs/screen/TabsScreen.ios.js
@@ -32,6 +32,13 @@ function TabsScreen(props) {
     style,
     ...filteredBaseProps
   } = baseProps;
+  // Compat shim: @react-navigation/bottom-tabs <=7.x passes `tabKey` (old API).
+  const effectiveScreenKey = filteredBaseProps.screenKey ?? filteredBaseProps.tabKey ?? '';
+  // bottom-tabs <=7.x passes icon as a flat PlatformIcon ({ios,android,shared})
+  // and selectedIcon as a PlatformIconIOS directly.
+  const legacyIcon = filteredBaseProps.icon;
+  const iosIcon = ios?.icon ?? legacyIcon?.ios ?? legacyIcon?.shared;
+  const iosSelectedIcon = ios?.selectedIcon ?? filteredBaseProps.selectedIcon;
   const {
     lifecycleCallbacks
   } = (0, _useTabsScreen.useTabsScreen)({
@@ -40,9 +47,9 @@ function TabsScreen(props) {
     onDidDisappear,
     onWillAppear,
     onWillDisappear,
-    screenKey: filteredBaseProps.screenKey
+    screenKey: effectiveScreenKey
   });
-  const iconProps = parseIconsToNativeProps(ios?.icon, ios?.selectedIcon);
+  const iconProps = parseIconsToNativeProps(iosIcon, iosSelectedIcon);
   return /*#__PURE__*/_react.default.createElement(_TabsScreenIOSNativeComponent.default, _extends({
     collapsable: false,
     style: [style, styles.fillParent]
@@ -51,6 +58,7 @@ function TabsScreen(props) {
     ref: componentNodeRef
   }, lifecycleCallbacks, iconProps, filteredBaseProps, {
     // iOS-specific
+    screenKey: effectiveScreenKey,
     isTitleUndefined: baseProps.title === null || baseProps.title === undefined,
     standardAppearance: mapAppearanceToNativeProp(ios?.standardAppearance),
     scrollEdgeAppearance: mapAppearanceToNativeProp(ios?.scrollEdgeAppearance),
diff --git a/node_modules/react-native-screens/lib/module/components/tabs/screen/TabsScreen.ios.js b/node_modules/react-native-screens/lib/module/components/tabs/screen/TabsScreen.ios.js
index 481a849..4d5619b 100644
--- a/node_modules/react-native-screens/lib/module/components/tabs/screen/TabsScreen.ios.js
+++ b/node_modules/react-native-screens/lib/module/components/tabs/screen/TabsScreen.ios.js
@@ -27,6 +27,13 @@ function TabsScreen(props) {
     style,
     ...filteredBaseProps
   } = baseProps;
+  // Compat shim: @react-navigation/bottom-tabs <=7.x passes `tabKey` (old API).
+  const effectiveScreenKey = filteredBaseProps.screenKey ?? filteredBaseProps.tabKey ?? '';
+  // bottom-tabs <=7.x passes icon as a flat PlatformIcon ({ios,android,shared})
+  // and selectedIcon as a PlatformIconIOS directly.
+  const legacyIcon = filteredBaseProps.icon;
+  const iosIcon = ios?.icon ?? legacyIcon?.ios ?? legacyIcon?.shared;
+  const iosSelectedIcon = ios?.selectedIcon ?? filteredBaseProps.selectedIcon;
   const {
     lifecycleCallbacks
   } = useTabsScreen({
@@ -35,9 +42,9 @@ function TabsScreen(props) {
     onDidDisappear,
     onWillAppear,
     onWillDisappear,
-    screenKey: filteredBaseProps.screenKey
+    screenKey: effectiveScreenKey
   });
-  const iconProps = parseIconsToNativeProps(ios?.icon, ios?.selectedIcon);
+  const iconProps = parseIconsToNativeProps(iosIcon, iosSelectedIcon);
   return /*#__PURE__*/React.createElement(TabsScreenIOSNativeComponent, _extends({
     collapsable: false,
     style: [style, styles.fillParent]
@@ -46,6 +53,7 @@ function TabsScreen(props) {
     ref: componentNodeRef
   }, lifecycleCallbacks, iconProps, filteredBaseProps, {
     // iOS-specific
+    screenKey: effectiveScreenKey,
     isTitleUndefined: baseProps.title === null || baseProps.title === undefined,
     standardAppearance: mapAppearanceToNativeProp(ios?.standardAppearance),
     scrollEdgeAppearance: mapAppearanceToNativeProp(ios?.scrollEdgeAppearance),
diff --git a/node_modules/react-native-screens/src/components/tabs/screen/TabsScreen.ios.tsx b/node_modules/react-native-screens/src/components/tabs/screen/TabsScreen.ios.tsx
index 6a13603..b71a75a 100644
--- a/node_modules/react-native-screens/src/components/tabs/screen/TabsScreen.ios.tsx
+++ b/node_modules/react-native-screens/src/components/tabs/screen/TabsScreen.ios.tsx
@@ -44,6 +44,17 @@ function TabsScreen(props: TabsScreenProps) {
     ...filteredBaseProps
   } = baseProps;
 
+  // Compat shim: @react-navigation/bottom-tabs <=7.x passes `tabKey` (old API).
+  const anyProps = filteredBaseProps as Record<string, unknown>;
+  const effectiveScreenKey = filteredBaseProps.screenKey ?? (anyProps['tabKey'] as string | undefined) ?? '';
+
+  // bottom-tabs <=7.x passes icon as a flat PlatformIcon ({ios,android,shared})
+  // and selectedIcon as a PlatformIconIOS directly.
+  type FlatIcon = {ios?: Parameters<typeof parseIconsToNativeProps>[0]; shared?: Parameters<typeof parseIconsToNativeProps>[0]};
+  const legacyIcon = anyProps['icon'] as FlatIcon | undefined;
+  const iosIcon = ios?.icon ?? legacyIcon?.ios ?? legacyIcon?.shared;
+  const iosSelectedIcon = ios?.selectedIcon ?? (anyProps['selectedIcon'] as Parameters<typeof parseIconsToNativeProps>[1]);
+
   const { lifecycleCallbacks } =
     useTabsScreen<TabsScreenIOSNativeComponentProps>({
       componentNodeRef,
@@ -51,10 +62,10 @@ function TabsScreen(props: TabsScreenProps) {
       onDidDisappear,
       onWillAppear,
       onWillDisappear,
-      screenKey: filteredBaseProps.screenKey,
+      screenKey: effectiveScreenKey,
     });
 
-  const iconProps = parseIconsToNativeProps(ios?.icon, ios?.selectedIcon);
+  const iconProps = parseIconsToNativeProps(iosIcon, iosSelectedIcon);
 
   return (
     <TabsScreenIOSNativeComponent
@@ -66,6 +77,7 @@ function TabsScreen(props: TabsScreenProps) {
       {...iconProps}
       {...filteredBaseProps}
       // iOS-specific
+      screenKey={effectiveScreenKey}
       isTitleUndefined={
         baseProps.title === null || baseProps.title === undefined
       }
