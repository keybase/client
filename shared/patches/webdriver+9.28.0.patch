diff --git a/node_modules/webdriver/build/index.js b/node_modules/webdriver/build/index.js
index 9dad7b9..0634eed 100644
--- a/node_modules/webdriver/build/index.js
+++ b/node_modules/webdriver/build/index.js
@@ -2226,8 +2226,10 @@ var WebDriverRequest = class {
     const searchParams = isBrowser ? void 0 : typeof options.queryParams === "object" ? options.queryParams : void 0;
     if (this.body && (Object.keys(this.body).length || this.method === "POST")) {
       requestOptions.body = JSON.stringify(this.body);
-      const contentLength = new TextEncoder().encode(requestOptions.body).length;
-      requestHeaders.set("Content-Length", "".concat(contentLength));
+      // PATCH (keybase): do NOT set Content-Length manually. On Node 26 the
+      // native-undici global dispatcher handles the request and rejects a
+      // manual Content-Length ("invalid content-length header" UND_ERR_INVALID_ARG),
+      // breaking every session create. undici computes it from the body itself.
     }
     let endpoint = this.endpoint;
     if (this.requiresSessionId) {
diff --git a/node_modules/webdriver/build/node.js b/node_modules/webdriver/build/node.js
index 46a9511..d382d37 100644
--- a/node_modules/webdriver/build/node.js
+++ b/node_modules/webdriver/build/node.js
@@ -2156,8 +2156,10 @@ var WebDriverRequest = class {
     const searchParams = isBrowser ? void 0 : typeof options.queryParams === "object" ? options.queryParams : void 0;
     if (this.body && (Object.keys(this.body).length || this.method === "POST")) {
       requestOptions.body = JSON.stringify(this.body);
-      const contentLength = new TextEncoder().encode(requestOptions.body).length;
-      requestHeaders.set("Content-Length", `${contentLength}`);
+      // PATCH (keybase): do NOT set Content-Length manually. On Node 26 the
+      // native-undici global dispatcher handles the request and rejects a
+      // manual Content-Length ("invalid content-length header" UND_ERR_INVALID_ARG),
+      // breaking every session create. undici computes it from the body itself.
     }
     let endpoint = this.endpoint;
     if (this.requiresSessionId) {
