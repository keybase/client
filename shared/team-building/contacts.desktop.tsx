import type * as T from '@/constants/types'

export const ContactsBanner = (_props: {
  namespace: T.TB.AllowedNamespace
  selectedService: T.TB.ServiceIdWithContact
  onRedoSearch: () => void
}) => null

export const ContactsImportButton = () => null
