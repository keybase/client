// @flow
import {type StylesDesktop} from '../styles'

export type Props = {
  forwardedRef?: any,
  src: string,
  style?: StylesDesktop,
  onDragStart?: (e: SyntheticEvent<>) => void,
  onLoad?: (e: SyntheticEvent<>) => void,
}
