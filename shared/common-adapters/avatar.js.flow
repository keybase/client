/* @flow */

import React, {Component} from 'react'

type CommonProps = {
  size: 180 | 110 | 75 | 60 | 45 | 32,
  style?: ?Object,
  onClick?: ?(() => void)
}

export type Props = ({url: ?string} & CommonProps) | ({username: ?string} & CommonProps)

declare export default class Avatar extends Component {
  props: Props;
}

