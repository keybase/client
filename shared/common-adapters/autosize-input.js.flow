// @flow
import {Component} from 'react'

export type Props = {
  autoFocus?: boolean,
  value: string,
  placeholder?: ?string,
  inputStyle?: Object,
  onChange: (text: string) => void,
  onKeyDown?: (ev: SyntheticKeyboardEvent<>) => void,
  onFocus?: (ev: SyntheticInputEvent<>) => void,
  onBlur?: (ev: SyntheticInputEvent<>) => void,
}

// TODO: This is used in only one place; move this out of
// common-adapters.

export default class AutosizeInput extends Component<Props> {
  focus: () => void
}
