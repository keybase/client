import type {SheetProps} from './index.shared'
export type {SheetProps} from './index.shared'

// Sheets are a mobile presentation; desktop callers use ModalCover or AnchoredPopup.
export const Sheet = (_p: SheetProps) => null

export default Sheet
