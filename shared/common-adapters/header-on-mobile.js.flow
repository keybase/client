// @flow
// import * as React from 'react'
// import {type StylesCrossPlatform} from '../styles'

// type HeaderOnMobileProps = {
// title?: string,
// onBack?: ?() => void,
// onCancel?: ?() => void,
// hideBackLabel?: boolean,
// headerStyle?: StylesCrossPlatform,
// customComponent?: ?React.Node,
// theme?: 'light' | 'dark', // defaults to light
// rightActionLabel?: string,
// onRightAction?: ?() => void,
// }

// TODO could try and enforce that the types match but for now we assume anything is ok

declare export default function HeaderOnMobile<P>(WrappedComponent: P): P
