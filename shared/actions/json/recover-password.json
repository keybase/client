{
  "prelude": [
    "import * as Types from '../constants/types/provision'"
  ],
  "actions": {
    "startRecoverPassword": {"username": "string"},

    "showDeviceListPage": {"devices": "Array<Types.Device>"},
    "submitDeviceSelect": {"id": "string"},
    "abortDeviceSelect": {},

    "showExplainDevice": {
      "type": ["'desktop'", "'mobile'"],
      "name": "string"
    },

    "submitResetPrompt": {"action": "boolean"},

    "showPaperKey": {},
    "displayPaperKeyError": {"error": "string"},
    "submitPaperKey": {"paperKey": "string"},
    "abortPaperKey": {},

    "displayError": {"error": "string"}
  }
}
