{
  "prelude": ["import type * as Types from '../constants/types/route-tree'"],
  "actions": {
    "navigateAppend": {
      "fromKey?": "string",
      "path": "Types.PathParam",
      "replace?": "boolean"
    },
    "navigateUpNoop": {
      "_description": "Nav up but no longer focused, for logging only"
    }
  }
}
