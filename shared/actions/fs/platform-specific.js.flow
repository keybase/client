// @flow
import * as Saga from '../../util/saga'

export function copyToDownloadDir(path: string, mime: string): Promise<*> {
  return new Promise((resolve, reject) => resolve())
}

export function* subSaga(): Saga.SagaGenerator<any, any> {}
