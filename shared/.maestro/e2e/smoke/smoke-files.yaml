appId: keybase.ios
---
- runFlow: ../../subflows/escape-to-tabs.yaml

- tapOn:
    text: "Files"

- extendedWaitUntil:
    visible:
      id: "files-browser"
    timeout: 3000

- assertVisible:
    id: "files-browser"
