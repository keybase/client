// @flow
import {Map} from 'immutable'
import * as React from 'react'

import type {RouteRenderStack, RouteProps} from '../route-tree/render-route'
import type {Reachable} from '../constants/types/flow-types'
import type {Tab} from '../constants/tabs'

export type Props = {
  appFocused: boolean,
  menuBadge: boolean,
  username: string,
  switchTab: (tab: Tab) => void,
  navigateUp: () => void,
  children: React.Node,
  routeSelected: Tab,
  routeStack: RouteRenderStack,
  hideNav: boolean,
  reachable: Reachable,
  navBadges: Map<Tab, number>,
}

export type StateProps = {
  _me: ?string,
  [key: string]: any,
}

export type DispatchProps = {
  _switchTab: (Tab, ?string) => void,
}

export type OwnProps = RouteProps<{}, {}>

export default class Nav extends React.Component<Props> {}
