// @flow
import React, {Component} from 'react'
import type {VisibleTab} from '../constants/tabs'

export type Props = {
  onTabClick: (tab: VisibleTab) => void,
  onSearchClick?: () => void,
  searchActive?: ?boolean,
  selectedTab: VisibleTab,
  username: string,
  badgeNumbers: {[key: VisibleTab]: number},
  searchContent?: React$Element,
  tabContent: {[key: VisibleTab]: React$Element}
}

export default class TabBar extends Component<void, Props, void> { }
