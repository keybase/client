// @flow
import * as React from 'react'
import {PeoplePageContent} from './index.shared'
import {type Props} from '.'

const People = (props: Props) => <PeoplePageContent {...props} />

export default People
