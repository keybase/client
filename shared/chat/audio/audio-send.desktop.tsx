// Desktop stub — audio send/record is mobile-only. AudioSendWrapper is imported by
// the chat input but only rendered in an isMobile branch, so a no-op is sufficient.
export const AudioSendWrapper = () => null
