// @flow
import {Component} from 'react'

import type {MessageID, TextMessage, ServerMessage} from '../../../constants/chat'

export type Props = {
  message: TextMessage,
  includeHeader: boolean,
  isFirstNewMessage: boolean,
  onRetry: (outboxID: string) => void,
  onAction: (message: ServerMessage, event: any) => void,
  style: Object,
  isSelected: boolean,
}

export default class MessageText extends Component<void, Props, void> { }
