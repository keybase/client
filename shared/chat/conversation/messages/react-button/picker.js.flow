// @flow
import * as React from 'react'

// Split Picker so we can use ReactDOM on the desktop side to manually
// give it focus.

export type Props = {|
  backgroundImageFn: (set: string, sheetSize: string) => string,
  onClick: ({colons: string}, evt: Event) => void,
|}

declare export class Picker extends React.Component<Props> {}
