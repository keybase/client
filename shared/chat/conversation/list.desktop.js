// @flow
import React from 'react'
import {Box, Text} from '../../common-adapters'
import {globalStyles} from '../../styles'

const Conversation = () => (
  <Box style={{...globalStyles.flexBoxRow, flex: 1}}>
    <Text type='Body'>List</Text>
  </Box>
)

export default Conversation
