// @flow
import {Component} from 'react'
import {List} from 'immutable'

import type {Message, ConversationIDKey, ParticipantItem} from '../../constants/chat'

export type Props = {
  isLoading: boolean,
  onLoadMoreMessages: () => void,
  messages: List<Message>,
  participants: List<ParticipantItem>,
  moreToLoad: boolean,
  onAddParticipant: () => void,
  onOpenFolder: () => void,
  onPostMessage: (text: string) => void,
  onShowProfile: (username: string) => void,
  participants: List<ParticipantItem>,
  selectedConversation: ?ConversationIDKey,
  sidePanelOpen: boolean,
  onToggleSidePanel: () => void,
}

export default class Conversation extends Component<void, Props, void> { }
