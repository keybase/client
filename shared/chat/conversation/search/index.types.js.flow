// @flow
import * as Types from '../../../constants/types/chat2'
export type Props = {|
  inProgress: boolean,
  loadSearchHit: number => void,
  onCancel: () => void,
  onSearch: string => void,
  totalResults: number,
|}
