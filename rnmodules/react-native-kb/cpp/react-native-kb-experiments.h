#pragma once

#ifndef KB_JSI_INBOUND_BINARY_MODE
#define KB_JSI_INBOUND_BINARY_MODE 0
#endif

#ifndef KB_JSI_OUTBOUND_TYPED_ARRAY_FASTPATH
#define KB_JSI_OUTBOUND_TYPED_ARRAY_FASTPATH 0
#endif

#ifndef KB_JSI_PERF
#define KB_JSI_PERF 0
#endif

#if KB_JSI_INBOUND_BINARY_MODE < 0 || KB_JSI_INBOUND_BINARY_MODE > 2
#error "KB_JSI_INBOUND_BINARY_MODE must be 0, 1, or 2"
#endif

#if KB_JSI_OUTBOUND_TYPED_ARRAY_FASTPATH != 0 && \
    KB_JSI_OUTBOUND_TYPED_ARRAY_FASTPATH != 1
#error "KB_JSI_OUTBOUND_TYPED_ARRAY_FASTPATH must be 0 or 1"
#endif

#if KB_JSI_PERF || KB_JSI_INBOUND_BINARY_MODE != 0 || \
    KB_JSI_OUTBOUND_TYPED_ARRAY_FASTPATH != 0
#define KB_JSI_EXPERIMENTS_ENABLED 1
#endif
