@namespace("keybase.1")

protocol block {
	import idl "common.avdl";

	void announceSession(string sid);
	bytes get(bytes blockid, UID uid);
	void delete(bytes blockid, UID uid);
	void put(bytes blockid, UID uid, bytes buf);
}
