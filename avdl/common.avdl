
@namespace("keybase.1")
protocol Common {

	record Status {
		int code;
		string name;
		string desc;
		array<string> fields;
	}

	fixed UID(16);
	
	record LoadUserArg {
		union { null, UID } uid;	
		union { null, string} username;
		boolean self;
	}
}
