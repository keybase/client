@namespace("keybase.1")
protocol NotifyRuntimeStats {
  import idl "common.avdl";

  record RuntimeStats {
    string cpu;
    string resident;
    string virt;
    string goheap;
    string goheapsys;
    string gostack;
  }
  void runtimeStatsUpdate(RuntimeStats stats) oneway; 
}