@namespace("keybase.1")
protocol appState {

  enum MobileAppState {
    FOREGROUND_0,
    BACKGROUND_1,
    INACTIVE_2,
    BACKGROUNDACTIVE_3
  }

  // gui -> service
  // desktop only
  // https://electronjs.org/docs/api/power-monitor
  void powerMonitorEvent(string event);
}
