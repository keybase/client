
@namespace("keybase.1")
protocol NotifySession {

  @notify("")
  void loggedOut(long version);
  void loggedIn(string username, boolean signedUp, long version); // signedUp if this is due to a signup
  void clientOutOfDate(string upgradeTo, string upgradeURI, string upgradeMsg);
}
