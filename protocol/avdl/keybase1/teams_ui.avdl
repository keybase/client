@namespace("keybase.1")
protocol teamsUi {
  boolean confirmRootTeamDelete(int sessionID, string teamName);
  boolean confirmSubteamDelete(int sessionID, string teamName);
  void teamListUnverifiedLoaded(int sessionID, AnnotatedTeamList list);
}
