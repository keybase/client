@namespace("keybase.1")
protocol avatars {
    import idl "common.avdl";

    @typedef("string") record Url {}
    @typedef("string") record Format {}

    record LoadUserAvatarsByUsernameRes {
        map<string, map<Format, Url>> picmap;
    }

    LoadUserAvatarsByUsernameRes loadUserAvatarsByUsername(array<string> usernames, array<Format> formats);
}