@namespace("keybase.1")

// implicitTeamUpgrade defines a protocol that service calls into KBFS for
// marking the start of migrating a TLF into iTeam backed form.
protocol implicitTeamUpgrade {
    void upgradeLock(string tlfName);
}
