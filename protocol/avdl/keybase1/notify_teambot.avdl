@namespace("keybase.1")
protocol NotifyTeambot {
  import idl "common.avdl";

  @notify("")
  void newTeambotKey(TeamID id, TeambotKeyGeneration generation);
  void teambotKeyNeeded(TeamID id, UID uid, TeambotKeyGeneration generation);
}
