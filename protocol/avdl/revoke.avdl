@namespace("keybase.1")

protocol revoke {
	import idl "common.avdl";

	void revokeKey(int sessionID, string id);
	void revokeDevice(int sessionID, DeviceID id);
	void revokeSigs(int sessionID, array<SigID> ids, array<int> seqnos);
}
