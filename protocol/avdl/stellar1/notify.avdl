@namespace("stellar.1")

protocol notify {
  import idl "common.avdl";

  @notify("")
  void paymentNotification(AccountID accountID, PaymentID paymentID);

  @notify("")
  void paymentStatusNotification(AccountID accountID, PaymentID paymentID, KeybaseTransactionID kbTxID, TransactionID txID);

  @notify("")
  void requestStatusNotification(KeybaseRequestID reqID);
}
