@namespace("keybase.1")

protocol device {
	import idl "common.avdl";

	array<Device> deviceList(int sessionID);
	
	void deviceAdd(string secretPhrase);

	// deviceAddCancel cancels an in-progress deviceAdd(...)
	void deviceAddCancel();
}
