@namespace("keybase.1")

protocol identify {
	import idl "common.avdl";
	import idl "identify_common.avdl";

	// set forceRemoteCheck to true to force all remote proofs to be checked and
	// not use any cached proof check results.
	IdentifyRes identify(int sessionID, string userAssertion, boolean trackStatement, boolean forceRemoteCheck);
	IdentifyRes identifyDefault(int sessionID, string userAssertion, boolean forceRemoteCheck);
}
