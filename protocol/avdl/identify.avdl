@namespace("keybase.1")

protocol identify {
	import idl "common.avdl";
	import idl "identify_common.avdl";

	IdentifyRes identify(
		int sessionID,
		UID uid,
		string username,
		boolean trackStatement,
		boolean luba,
		boolean loadSelf
	);

	IdentifyRes identifyDefault(
		int sessionID,
		string username
	);
}
