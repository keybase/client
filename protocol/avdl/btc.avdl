@namespace("keybase.1")

protocol BTC {
	import idl "common.avdl";

	/**
	  Claim a bitcoin address.
	 */
	@command({"name": "btc"})
	void registerBTC(int sessionID, string address, boolean force=false);
}
