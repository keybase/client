
@namespace("keybase.1")
protocol loginUi {
	import idl "common.avdl";

	string getEmailOrUsername(int sessionID);
	boolean promptRevokeBackupDeviceKeys(int sessionID, Device device);
	void displayBackupPhrase(int sessionID, string phrase);
}
