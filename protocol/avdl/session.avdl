@namespace("keybase.1")

protocol session {
	import idl "common.avdl";

	record Session {
		UID uid;
		string username;
		string token;
		string deviceSibKid;
		string deviceSubKid;
	}

	Session currentSession();
}
