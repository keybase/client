
@namespace("keybase.1")
protocol gregorUI {
  import idl "github.com/keybase/gregor/protocol/gregor1" as gregor1;
  void pushMessages(array<gregor1.Message> messages);
  void reconnected() oneway;
}
