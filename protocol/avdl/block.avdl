@namespace("keybase.1")

protocol block {
	import idl "common.avdl";

	void blockSession(string sid);
	bytes getBlock(string blockId, int size);
	void putBlock(string blockId, bytes buf);

	//future RPC from bindex to bserver
	void delBlock(string blockId, int size);
}
