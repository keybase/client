@namespace("keybase.1")

protocol block {
	import idl "common.avdl";

	void blockSession(string sid);
	bytes getBlock(bytes blockid, int size);
	void putBlock(bytes blockid, bytes buf);
	void delBlock(bytes blockid, int size);
}
