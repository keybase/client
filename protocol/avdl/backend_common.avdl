@namespace("keybase.1")

protocol backendCommon {
	import idl "common.avdl";

	record BlockIdCombo{
		string blockHash;
		int size;
		UID chargedTo;
	}

	record BlockKey{
		int epochID;
		string epochKey;
		string randBlockId;
		string blockKey;
	}

}


